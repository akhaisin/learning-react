export function getVisibleRange(_scrollTop: number, _rowHeight: number, _containerHeight: number, _totalItems: number) {
  // TODO: Calculate the visible start and end indexes.
  return { startIndex: 0, endIndex: 0 };
}