import styles from './Component.module.css';

function TodoList() {
  return (
    <section className={styles.container}>
      <div className={styles.inputRow}>
        {/* TODO: Replace this with reducer-managed state and actions. */}
      </div>
    </section>
  );
}

export default TodoList;