import ProgressBar from '../../components/progressBar/ProgressBar';

function ProgressBarDemo() {
  return <ProgressBar start={0} stop={100} step={1} interval={50} />;
}

export default ProgressBarDemo;
